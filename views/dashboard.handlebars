{{#if loggedIn}}
{{#each posts as |post|}}
{{> post-details}}
{{/each}}
{{else}}
<section class="container">
    <section class="justify-center">
        <h2 class="post-title">Please Log In or Sign Up!</h3>
    </section>
</section>
{{/if}}